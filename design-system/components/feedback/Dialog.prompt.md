Centered modal over an ink veil with warm blur. Positioned `absolute` inside its nearest positioned ancestor so it works inside kit screens.

```jsx
<Dialog title="Tu rutina quedó lista" description="Te enviamos el recordatorio diario a tu correo."
  footer={<Button>Entendido</Button>} onClose={close} />
```

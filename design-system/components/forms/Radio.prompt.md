One-of-many choice; controlled via `checked` + shared `name`. Use for plan / frequency pickers.

```jsx
<Radio name="plan" label="Suscripción mensual" description="Ahorra 15% y nunca te quedas sin frasco." checked />
```

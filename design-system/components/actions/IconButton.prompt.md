Circular single-glyph action for toolbars, carousels, cart steppers and dialog closes.

```jsx
<IconButton icon="x" label="Cerrar" variant="ghost" />
<IconButton icon="shopping-bag" label="Bolsa" variant="solid" />
```

Always circular (`--radius-pill`); `label` is mandatory.
